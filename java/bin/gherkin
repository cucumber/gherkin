#!/usr/bin/env bash
set -euf -o pipefail

mvn exec:java --quiet --batch-mode -Dexec.mainClass="io.cucumber.gherkin.Main" -Dexec.classpathScope="test" -Dexec.args="$*" \
  | sed 's/\x1b\[0m//g' # Remove ANSI resets printed out by maven
